plugins{
    id 'java-library'
}
springBoot{
    mainClass = 'fairman.aidan.ToolRentalApplication'
}
dependencies{
    api project(':api')
    implementation 'org.springframework.boot:spring-boot-starter-web'
    runtimeOnly 'com.h2database:h2'
}